<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- TODO: заменить на <queries> с пакетом WMS (см. ниже) и удалить это разрешение.
         Оставлено, чтобы не сломать текущее поведение, пока не известен пакет WMS. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- TODO: раскомментировать и подставить реальный пакет WMS
    <queries>
        <package android:name="PUT.WMS.PACKAGE.HERE" />
    </queries>
    -->

    <!-- Требуется для команды restart. Выдаётся только системному/привилегированному приложению.
         TODO: решить — Device Owner (DevicePolicyManager.reboot), priv-app или SDK вендора.
         Fallback через su убрать. -->
    <uses-permission android:name="android.permission.REBOOT"
        tools:ignore="ProtectedPermissions" />

    <application
        android:name=".WakeUpApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WakeUpMessenger">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.WakeUpMessenger">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.BatterySetupActivity"
            android:exported="false"
            android:excludeFromRecents="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.WakeUpMessenger" />

        <service
            android:name=".service.WakeUpService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:stopWithTask="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Постоянное XMPP-соединение с сервером склада для приёма команд и сообщений на ТСД" />
        </service>

        <!-- Системные броадкасты доставляются и при exported=false; чужие приложения
             больше не могут слать этому ресиверу поддельные интенты.
             QUICKBOOT_* убраны: добавлять только после проверки на конкретной модели ТСД. -->
        <receiver
            android:name=".service.BootReceiver"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
